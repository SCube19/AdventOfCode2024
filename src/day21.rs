
use std::fs;
use crate::solver::Solver;

pub struct Day21;
impl Solver<i32, i32> for Day21 {
    fn solve_part1(&self) -> i32 {
        let file: String = fs::read_to_string("input/day21.txt").unwrap();

        return 0;
    }

    fn solve_part2(&self) -> i32 {
        let file: String = fs::read_to_string("input/day21.txt").unwrap();

        return 0;
    }
}
